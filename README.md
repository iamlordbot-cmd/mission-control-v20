# Mission Control V20 — Space Station Interior

Ultra-immersive Mission Control dashboard (React + Tailwind + React Three Fiber + Framer Motion).

## Run

```bash
npm install --include=dev
npm run dev
```

## Build

```bash
npm run build
npm run preview
```

## Notes
- 8 sections only (no "Usage/Coûts").
- Every mocked metric/value is tagged with **(mock)**.
- Connection warnings include **reason + action**.
